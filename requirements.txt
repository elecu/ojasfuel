streamlit>=1.35
streamlit-javascript>=0.1.5
openfoodfacts>=5.0
requests>=2.31
Pillow>=10.0
deep-translator>=1.11

# Optional — barcode detection (requires system lib: sudo apt install libzbar0)
pyzbar>=0.1.9

# Optional — OCR (first run downloads ~1GB models)
easyocr>=1.7
opencv-python>=4.9
